//
// Created by klw2 on 5/1/2018.
//

#include "bots.h"

box

bots::bots(double len, double w, double h){
    setLength(len);
    setWidth(w);
    setHeight(h);
}

void bots::setLength(const double & len){

}